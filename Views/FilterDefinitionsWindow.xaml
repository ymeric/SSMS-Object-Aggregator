<Window x:Class="SSMS.ObjectAggregator.Views.FilterDefinitionsWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filter Definitions"
        Height="420"
        Width="640"
        WindowStartupLocation="CenterOwner">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Actions">
                <MenuItem Header="_Insert" Command="{Binding InsertCommand}" />
                <MenuItem Header="_Edit" Command="{Binding EditCommand}" />
                <MenuItem Header="_Delete" Command="{Binding DeleteCommand}" />
            </MenuItem>
        </Menu>

        <ToolBarTray DockPanel.Dock="Top" IsLocked="True">
            <ToolBar>
                <Button Command="{Binding InsertCommand}" ToolTip="Insert" Margin="2">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="16" />
                </Button>
                <Button Command="{Binding EditCommand}" ToolTip="Edit" Margin="2">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" FontSize="16" />
                </Button>
                <Button Command="{Binding DeleteCommand}" ToolTip="Delete" Margin="2">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="16" />
                </Button>
            </ToolBar>
        </ToolBarTray>

        <DataGrid ItemsSource="{Binding Filters}"
                  SelectedItem="{Binding SelectedFilter, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="8"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Insert"
                                          Command="{Binding InsertCommand}" />
                                <MenuItem Header="Edit"
                                          Command="{Binding EditCommand}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                <MenuItem Header="Delete"
                                          Command="{Binding DeleteCommand}"
                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Schema Name Filter" Binding="{Binding SchemaFilter}" Width="*" />
                <DataGridTextColumn Header="Object Name Filter" Binding="{Binding ObjectFilter}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>